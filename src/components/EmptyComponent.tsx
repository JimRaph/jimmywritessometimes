import React from 'react';

// check docusaurus.config.ts, you will see a plugin that uses this component.
// that is the only purpose this serves
export default function EmptyComponent() {
  return null;
}